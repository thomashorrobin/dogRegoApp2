<%= form_for(@price_plan) do |f| %>
  <% if @price_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price_plan.errors.count, "error") %> prohibited this price_plan from being saved:</h2>

      <ul>
      <% @price_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :months %><br>
    <%= f.number_field :months %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.number_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
